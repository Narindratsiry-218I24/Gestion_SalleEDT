@model Gestion_SalleClasseEDT.Models.ViewModels.CoursListViewModel
@{
    ViewBag.Title = "Cours";
    var mentions = ViewBag.Mentions as List<Gestion_SalleClasseEDT.Models.Mention> ?? new List<Gestion_SalleClasseEDT.Models.Mention>();
    var niveaux = ViewBag.Niveaux as List<Gestion_SalleClasseEDT.Models.Niveau> ?? new List<Gestion_SalleClasseEDT.Models.Niveau>();
    var statuts = ViewBag.Statuts as List<string> ?? new List<string>();
}

<div class="space-y-6 animate-in fade-in zoom-in-95 duration-500 relative h-full flex flex-col">
    <!-- Header -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
        <div>
            <h1 class="text-3xl font-black text-[#17203A] tracking-tight">Gestion des Cours</h1>
            <p class="text-gray-500 mt-1">Filtrez, recherchez et gérez tous les cours de l'établissement.</p>
        </div>
        <div class="flex gap-2">
            <button class="flex items-center gap-2 bg-[#17203A] hover:bg-[#12192B] text-white px-4 py-2 rounded-lg transition-colors font-medium" onclick="openCreateCourseModal()">
                <i data-lucide="plus" class="w-4 h-4"></i> Nouveau Cours
            </button>
        </div>
    </div>

    <!-- Filtres -->
    <div class="bg-white rounded-xl p-5 shadow-sm border border-gray-100">
        <form method="get" action="@Url.Action("Cours")" id="filterForm" class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <div>
                <label class="block text-xs font-medium text-gray-700 mb-1">Mention</label>
                <select name="mention" id="filterMention" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <option value="">Toutes les mentions</option>
                    @foreach (var m in mentions)
                    {
                        <option value="@m.NomMention" selected="@(Model?.FilterMention == m.NomMention)">
                            @m.NomMention
                        </option>
                    }
                </select>
            </div>

            <div>
                <label class="block text-xs font-medium text-gray-700 mb-1">Niveau</label>
                <select name="niveau" id="filterNiveau" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <option value="">Tous les niveaux</option>
                    @foreach (var n in niveaux.OrderBy(n => n.Ordre))
                    {
                        var niveauName = n.CodeNiveau switch
                        {
                            "L1" => "Licence 1",
                            "L2" => "Licence 2",
                            "L3" => "Licence 3",
                            "M1" => "Master 1",
                            "M2" => "Master 2",
                            _ => n.CodeNiveau
                        };
                        <option value="@niveauName" selected="@(Model?.FilterNiveau == niveauName)">
                            @niveauName
                        </option>
                    }
                </select>
            </div>

            <div>
                <label class="block text-xs font-medium text-gray-700 mb-1">Parcours</label>
                <select name="parcours" id="filterParcours" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <option value="">Tous les parcours</option>
                </select>
            </div>

            <div>
                <label class="block text-xs font-medium text-gray-700 mb-1">Statut</label>
                <select name="statut" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <option value="">Tous les statuts</option>
                    @foreach (var s in statuts)
                    {
                        <option value="@s" selected="@(Model?.FilterStatut == s)">
                            @s
                        </option>
                    }
                </select>
            </div>

            <div class="md:col-span-3">
                <label class="block text-xs font-medium text-gray-700 mb-1">Recherche</label>
                <div class="relative">
                    <i data-lucide="search" class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400"></i>
                    <input type="text" name="search" value="@Model?.SearchTerm" placeholder="Rechercher par matière, code, enseignant..." 
                           class="w-full pl-9 pr-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none" />
                </div>
            </div>

            <div class="flex items-end gap-2">
                <button type="submit" class="w-full bg-[#17203A] hover:bg-[#12192B] text-white px-4 py-2 rounded-lg transition-colors font-medium">
                    <i data-lucide="filter" class="w-4 h-4 inline mr-1"></i> Filtrer
                </button>
                <button type="reset" class="px-4 py-2 border border-gray-200 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors" onclick="resetFilters()">
                    <i data-lucide="x" class="w-4 h-4"></i>
                </button>
            </div>
        </form>
    </div>

    <!-- Résumé des filtres actifs -->
    @if (!string.IsNullOrEmpty(Model?.FilterMention) || !string.IsNullOrEmpty(Model?.FilterNiveau) || !string.IsNullOrEmpty(Model?.FilterParcours))
    {
        <div class="flex flex-wrap gap-2 items-center text-sm text-gray-600 bg-gray-50 p-3 rounded-lg">
            <span class="font-medium text-gray-700">Filtres actifs :</span>
            @if (!string.IsNullOrEmpty(Model.FilterMention))
            {
                <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-xs">
                    Mention : @Model.FilterMention
                    <a href="@Url.Action("Cours", new { niveau = Model.FilterNiveau, parcours = Model.FilterParcours, search = Model.SearchTerm, statut = Model.FilterStatut })" class="ml-1 text-blue-600 hover:text-blue-800">×</a>
                </span>
            }
            @if (!string.IsNullOrEmpty(Model.FilterNiveau))
            {
                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-xs">
                    Niveau : @Model.FilterNiveau
                    <a href="@Url.Action("Cours", new { mention = Model.FilterMention, parcours = Model.FilterParcours, search = Model.SearchTerm, statut = Model.FilterStatut })" class="ml-1 text-green-600 hover:text-green-800">×</a>
                </span>
            }
            @if (!string.IsNullOrEmpty(Model.FilterParcours))
            {
                <span class="bg-purple-100 text-purple-800 px-3 py-1 rounded-full text-xs">
                    Parcours : @Model.FilterParcours
                    <a href="@Url.Action("Cours", new { mention = Model.FilterMention, niveau = Model.FilterNiveau, search = Model.SearchTerm, statut = Model.FilterStatut })" class="ml-1 text-purple-600 hover:text-purple-800">×</a>
                </span>
            }
            @if (!string.IsNullOrEmpty(Model.SearchTerm))
            {
                <span class="bg-gray-200 text-gray-700 px-3 py-1 rounded-full text-xs">
                    Recherche : "@Model.SearchTerm"
                    <a href="@Url.Action("Cours", new { mention = Model.FilterMention, niveau = Model.FilterNiveau, parcours = Model.FilterParcours, statut = Model.FilterStatut })" class="ml-1 text-gray-600 hover:text-gray-800">×</a>
                </span>
            }
        </div>
    }

    <!-- Table des cours -->
    <partial name="_CoursTable" model="Model" />

    <!-- Modal de création de cours (Étape 1 du Wizard) -->
    <div id="createCourseModal" class="fixed inset-0 z-50 hidden bg-[#0B0F19]/60 backdrop-blur-sm transition-all duration-300 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl border border-gray-100 shadow-2xl w-full max-w-2xl max-h-[90vh] overflow-y-auto animate-in fade-in zoom-in-95 duration-200">
            <div class="sticky top-0 z-10 flex items-center justify-between p-5 border-b bg-white rounded-t-2xl">
                <div>
                    <span class="text-xs uppercase tracking-wider text-indigo-600 font-bold">Étape 1 : Définition du cours</span>
                    <h3 class="text-xl font-black text-[#17203A] mt-0.5">Créer un nouveau cours</h3>
                </div>
                <button onclick="closeCreateCourseModal()" class="p-2 hover:bg-gray-100 rounded-xl transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>
            <div class="p-6">
                <form id="createCourseForm" class="space-y-6">
                    @Html.AntiForgeryToken()
                    
                    <!-- Groupe 1: Contenu pédagogique -->
                    <div class="bg-gray-50/50 rounded-xl p-5 border border-gray-100 space-y-4">
                        <h4 class="text-sm font-bold text-gray-800 flex items-center gap-2">
                            <i data-lucide="book-open" class="text-indigo-600 w-4 h-4"></i>
                            1. Contenu Pédagogique
                        </h4>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div class="col-span-2">
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Matière <span class="text-red-500">*</span></label>
                                <select id="courseMatiereId" name="IdMatiere" required class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="">Sélectionnez une matière</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Type de cours</label>
                                <select id="courseType" name="TypeCours" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="CM">Cours Magistral (CM)</option>
                                    <option value="TD">Travaux Dirigés (TD)</option>
                                    <option value="TP">Travaux Pratiques (TP)</option>
                                    <option value="Projet">Projet</option>
                                    <option value="Seminaire">Séminaire</option>
                                    <option value="Examen">Examen</option>
                                    <option value="Soutenance">Soutenance</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Volume Horaire <span class="text-red-500">*</span></label>
                                <input type="number" id="courseVolumeHours" name="VolumeHours" required min="1" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" placeholder="Ex: 30" oninput="updateCreationSummary()" />
                            </div>
                            <div class="col-span-2">
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Capacité (Optionnel)</label>
                                <input type="number" id="courseCapacity" name="Capacity" min="1" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" placeholder="Ex: 40" oninput="updateCreationSummary()" />
                            </div>
                        </div>
                    </div>

                    <!-- Groupe 2: Affectation -->
                    <div class="bg-gray-50/50 rounded-xl p-5 border border-gray-100 space-y-4">
                        <h4 class="text-sm font-bold text-gray-800 flex items-center gap-2">
                            <i data-lucide="users" class="text-indigo-600 w-4 h-4"></i>
                            2. Affectation &amp; Cible
                        </h4>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Enseignant</label>
                                <select id="courseProfesseurId" name="IdProfesseur" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="">Non assigné</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Semestre</label>
                                <select id="courseSemestreId" name="IdSemestre" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="">Sélectionnez un semestre</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Classe cible</label>
                                <select id="courseClasseId" name="IdClasse" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="">Non assignée</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Groupe (Optionnel)</label>
                                <select id="courseGroupeId" name="IdGroupe" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none" onchange="updateCreationSummary()">
                                    <option value="">Aucun groupe</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <!-- Groupe 3: Modalités -->
                    <div class="bg-gray-50/50 rounded-xl p-5 border border-gray-100 space-y-4">
                        <h4 class="text-sm font-bold text-gray-800 flex items-center gap-2">
                            <i data-lucide="settings" class="text-indigo-600 w-4 h-4"></i>
                            3. Modalités d'Enseignement
                        </h4>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Mode d'enseignement</label>
                                <select id="courseMode" name="Mode" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none">
                                    <option value="Presentiel">Présentiel</option>
                                    <option value="EnLigne">En ligne</option>
                                    <option value="Hybride">Hybride</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold uppercase text-gray-500 mb-1 font-semibold">Langue</label>
                                <select id="courseLanguage" name="LanguageStr" class="w-full px-3 py-2 bg-white border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:outline-none">
                                    <option value="Francais">Français</option>
                                    <option value="Anglais">Anglais</option>
                                    <option value="Malagasy">Malagasy</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <!-- Live Summary Panel -->
                    <div class="bg-indigo-50/40 rounded-xl p-4 border border-indigo-100 flex flex-col gap-1 text-sm text-gray-700">
                        <span class="text-xs uppercase tracking-wider text-indigo-500 font-bold">Résumé Live du Cours</span>
                        <div id="courseCreationSummary" class="font-semibold text-indigo-950">
                            Veuillez renseigner les champs ci-dessus.
                        </div>
                    </div>

                    <div class="flex justify-end gap-3 pt-4 border-t border-gray-100">
                        <button type="button" onclick="closeCreateCourseModal()" class="px-5 py-2.5 border border-gray-200 rounded-xl text-sm font-semibold text-gray-600 hover:bg-gray-50 transition-colors">
                            Annuler
                        </button>
                        <button type="submit" class="px-5 py-2.5 bg-[#17203A] text-white rounded-xl hover:bg-[#12192B] transition-colors font-semibold flex items-center gap-1.5">
                            Créer et planifier
                            <i data-lucide="arrow-right" class="w-4 h-4"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>


</div>

@section Scripts {
    <script>
        // Configuration des parcours par mention et niveau
        const parcoursConfig = {
            "Informatique": {
                "Licence": ["DA2I"],
                "Master": ["M2I", "SDIA", "SIGD"]
            },
            "Management": {
                "Licence": ["AES"],
                "Master": ["AE", "MEAS"]
            },
            "Multimédia": {
                "Licence": ["ICM"],
                "Master": ["RPO", "RCPO"]
            }
        };

        // --- Filtres dépendants ---
        document.addEventListener('DOMContentLoaded', function() {
            const mentionSelect = document.getElementById('filterMention');
            const niveauSelect = document.getElementById('filterNiveau');
            const parcoursSelect = document.getElementById('filterParcours');

            function updateParcoursOptions() {
                const mention = mentionSelect.value;
                const niveau = niveauSelect.value;
                
                parcoursSelect.innerHTML = '<option value="">Tous les parcours</option>';
                
                if (mention && niveau) {
                    const niveauType = niveau.startsWith("Licence") ? "Licence" : "Master";
                    const parcours = parcoursConfig[mention]?.[niveauType] || [];
                    
                    parcours.forEach(p => {
                        const opt = document.createElement('option');
                        opt.value = p;
                        opt.textContent = p;
                        parcoursSelect.appendChild(opt);
                    });

                    // Sélectionner le parcours actuel s'il existe
                    const currentParcours = '@Model?.FilterParcours';
                    if (currentParcours) {
                        parcoursSelect.value = currentParcours;
                    }
                }
            }

            mentionSelect.addEventListener('change', updateParcoursOptions);
            niveauSelect.addEventListener('change', updateParcoursOptions);

            // Initialiser les parcours si mention et niveau sont déjà sélectionnés
            if (mentionSelect.value && niveauSelect.value) {
                updateParcoursOptions();
            }
        });

        </script>
}   


